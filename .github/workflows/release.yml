name: Build and Release

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag (e.g. v1.0.0)'
        required: true

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - run: npm i

      - run: npm run build

      - name: Get package name
        id: pkg
        run: echo "name=$(node -p 'require(\"./package.json\").name')" >> "$GITHUB_OUTPUT"

      - name: Zip dist
        run: cd dist && zip -r ../${{ steps.pkg.outputs.name }}-${{ inputs.tag }}.zip . && cd ..

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ inputs.tag }}
          files: ${{ steps.pkg.outputs.name }}-${{ inputs.tag }}.zip
